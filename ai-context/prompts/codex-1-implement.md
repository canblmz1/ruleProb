# AI Agent Prompt — Implementer (Codex #1)

> Generated by ai-dev orchestrator (manual-runner mode)
> Target project: ruleProb
> Date: 2026-04-27 20:23 UTC

---


## Role
You are Codex #1 Implementer. Your job is to implement ALL sprint tasks, not just one.

## Sprint Context
This sprint includes 1 task(s). Implement all of them unless you hit a Human Decision Point.

## All Sprint Tasks
- [SEC-001] Block shell-metacharacter command-injection bypass in action executor (security, P1)

## Human Decision Points
- None

## AI Can Continue Until
AI can implement SEC-001 end-to-end: edit src/actions/execute.ts to reject
shell metacharacters in `isCommandAllowed`, switch the `execa` call to
`shell: false` with tokenized args, extend `FORBIDDEN_CMD_REGEX` to cover
`node`/`python`/`perl`/`ruby`/`awk`/`tee`/`xargs`/`eval`/`exec`, and extend
`tests/instructions.test.ts` with the new bypass cases enumerated in the
patch plan. AI may also run `pnpm build` and `pnpm test` to verify and may
follow up with SEC-002 and SEC-003 in separate commits if time permits.

AI must stop before:
- removing or weakening any of the existing path-traversal / forbidden-path
  / forbidden-command checks
- changing public CLI flags, provider names, or report schemas
- editing `.env`, `.env.example`, or any other secret-bearing file
- pushing to a remote, opening a PR, or tagging a release without explicit
  user approval
- adopting a process/OS-level sandbox (containers, firejail, sandbox-exec) —
  that is SEC-004 follow-up work that needs an architecture decision first

## Context Files to Read
- C:\dev\ruleProb\ai-context\PROJECT_BRIEF.md
- C:\dev\ruleProb\ai-context\CURRENT_BEHAVIOR.md
- C:\dev\ruleProb\ai-context\RULES.md
- ai-context\security\PATCH_PLAN.md
- C:\dev\ruleProb\ai-context\PLAN.md

## Evidence / Problem Statement
Not provided in PLAN.md. Do not infer; inspect referenced source/RFC before implementing.

## Acceptance Criteria
Not provided in PLAN.md. Do not infer; inspect referenced source/RFC before implementing.

## Sprint Implementation Rules
- Implement ALL listed sprint tasks.
- Do not stop just because there are multiple tasks.
- Use local/mocked/test-mode implementations when real services are unavailable.
- If real API keys, production deploy, payment setup, trading, or real user data is required, STOP at that boundary.
- Document the remaining items as Human Decision Points.
- Do not invent secrets, API keys, product IDs, webhook URLs, or production config.
- Do not ask the user mid-task unless a real-world decision is required.

## Hard Rules
- Do not fix unrelated issues outside the sprint scope.
- Do not refactor unrelated code.
- Do not edit secret files (.env, .env.*, *.pem, *.key, secrets.*).
- If another issue is discovered, write it to C:\dev\ruleProb\ai-context\FOLLOW_UPS.md.
- Keep the diff manageable.
- Preserve default behavior unless the RFC explicitly allows change.
- Run relevant tests if possible.
- Do not claim tests passed unless they actually ran.

## Allowed Files
- Source files relevant to the sprint tasks.
- Test files.

## Forbidden Files
- .env, .env.*, *.pem, *.key, secrets.*, id_rsa, id_ed25519
- Generated directories: node_modules, dist, build, .next, .venv

## Test Command
Not provided in PLAN.md. Do not infer; inspect referenced source/RFC before implementing.

## Output
Write implementation report to:
- C:\dev\ruleProb\ai-context\handoffs\codex-1-implementation-report.md

## Report Must Include
- all task IDs implemented
- files changed
- summary of what was completed
- tests run
- test result
- what was NOT completed (if anything) and why
- risks
- follow-ups
- rollback notes

## Stop Condition
Stop when all sprint tasks are complete OR when a Human Decision Point is reached.
