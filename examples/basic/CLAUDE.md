# Project Instructions

- Use pnpm, not npm or yarn.
- Never edit files under src/generated/.
- Always run pnpm test before final response.
- Do not modify package.json without explicit approval.
- Never use `any` in TypeScript code.
- In the final answer, mention changed files.
